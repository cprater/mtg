import { ADD_MANA_TO_POOL } from '../actions';

export default function manaPoolReducer(state, action) {
  const { type, producer, mana_type } = action;


  if (type === ADD_MANA_TO_POOL) {





  }

  return state.players;
}
